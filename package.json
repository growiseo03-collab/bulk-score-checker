{
  "name": "bulk-site-score-checker",
  "version": "1.0.0",
  "private": true,
  "description": "Free bulk DA/DR/Spam Score style checker + website analyzer, deployed on Cloudflare Pages.",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2024-01-01"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
